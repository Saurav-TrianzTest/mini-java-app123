# Mini Java App Configuration - Cloud Ready Version
# All sensitive values should be provided via environment variables

# Cloud-ready: Server configuration using environment variables
server.port=${SERVER_PORT:8080}
server.host=${SERVER_HOST:0.0.0.0}
server.context-path=${SERVER_CONTEXT_PATH:/mini-app}

# Cloud-ready: Database configuration using environment variables
database.url=${DATABASE_URL:jdbc:mysql://localhost:3306/mini_app_db?useSSL=true&serverTimezone=UTC}
database.username=${DB_USERNAME:root}
database.password=${DB_PASSWORD:password123}
database.driver=${DB_DRIVER:com.mysql.cj.jdbc.Driver}
database.pool.max-connections=${DB_POOL_SIZE:20}
database.pool.timeout=${DB_POOL_TIMEOUT:5000}
database.pool.min-idle=${DB_POOL_MIN_IDLE:5}
database.connection.timeout=${DB_CONNECTION_TIMEOUT:30000}
database.idle.timeout=${DB_IDLE_TIMEOUT:600000}
database.max.lifetime=${DB_MAX_LIFETIME:1800000}

# Cloud-ready: Cache configuration using environment variables
cache.redis.host=${REDIS_HOST:127.0.0.1}
cache.redis.port=${REDIS_PORT:6379}
cache.redis.password=${REDIS_PASSWORD:}
cache.redis.database=${REDIS_DATABASE:0}
cache.timeout=${CACHE_TIMEOUT:5000}

# Cloud-ready: External service URLs using environment variables
external.api.base-url=${EXTERNAL_API_URL:http://api.example.com:8080/v1}
external.api.timeout=${API_TIMEOUT:30000}
external.api.key=${EXTERNAL_API_KEY:}

payment.service.url=${PAYMENT_SERVICE_URL:https://payment.internal.company.com/process}
payment.service.username=${PAYMENT_SERVICE_USERNAME:}
payment.service.password=${PAYMENT_SERVICE_PASSWORD:}

# Cloud-ready: Use environment variables for directories (cloud storage paths)
app.config.directory=${APP_CONFIG_DIR:/app/config}
app.log.directory=${APP_LOG_DIR:/app/logs}
app.temp.directory=${APP_TEMP_DIR:/tmp}
app.upload.directory=${APP_UPLOAD_DIR:/app/uploads}

# Cloud-ready: Security configuration using environment variables
security.jwt.secret=${JWT_SECRET:}
security.admin.username=${ADMIN_USERNAME:}
security.admin.password=${ADMIN_PASSWORD:}
security.encryption.key=${ENCRYPTION_KEY:}

# Cloud-ready: Monitoring endpoints using environment variables
monitoring.endpoint=${MONITORING_ENDPOINT:http://monitoring.internal.company.com:9090/metrics}
monitoring.username=${MONITORING_USERNAME:}
monitoring.password=${MONITORING_PASSWORD:}

# Cloud-ready: Messaging configuration using environment variables
messaging.rabbitmq.host=${RABBITMQ_HOST:rabbitmq.internal.company.com}
messaging.rabbitmq.port=${RABBITMQ_PORT:5672}
messaging.rabbitmq.username=${RABBITMQ_USERNAME:}
messaging.rabbitmq.password=${RABBITMQ_PASSWORD:}

# Cloud-ready: Environment configuration using environment variables
environment=${APP_ENVIRONMENT:production}
debug.enabled=${DEBUG_ENABLED:false}
logging.level=${LOG_LEVEL:INFO}

# Cloud-ready: Health check and readiness probe configurations
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
management.health.probes.enabled=true

# Cloud-ready: Timeout configurations for cloud environments
server.connection-timeout=${SERVER_CONNECTION_TIMEOUT:20000}
server.socket.timeout=${SERVER_SOCKET_TIMEOUT:30000}
query.timeout=${DB_QUERY_TIMEOUT:30}